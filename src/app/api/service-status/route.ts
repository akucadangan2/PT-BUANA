// TODO: endpoint update status service (dipanggil dari app teknisi)
// POST { service_request_id, status, note?, photo_url? }
export async function POST(request: Request) {
  return new Response('TODO')
}
