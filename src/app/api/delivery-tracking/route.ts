// TODO: endpoint update lokasi kurir real-time (dipanggil dari app kurir)
// POST { delivery_id, lat, lng } -> update kolom current_lat/current_lng di tabel deliveries
export async function POST(request: Request) {
  return new Response('TODO')
}
